/// Enum representing the type of feed item.
enum FeedItemType {
  /// A track was posted by the original artist.
  trackPosted,

  /// A playlist was posted by a user.
  playlistPosted,

  /// A track was reposted by another user.
  repost,
}
